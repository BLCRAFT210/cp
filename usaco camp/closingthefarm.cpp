#include <bits/stdc++.h>
using namespace std;

vector<int> adj[3001];
bool closed[3001];
int n,m;



int main()
{
   ios_base::sync_with_stdio(false);
   cin.tie(NULL);
   
   cin >> n >> m;
   for (int i = 0;i<m;i++)
   {
      int a,b;
      cin >> a >> b;
      a--;
      b--;
      
   }
}